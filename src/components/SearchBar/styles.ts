import styled from "styled-components";

export const Container = styled.div `

    input {
        background: transparent;
        border: 0;
        position: absolute;
        margin-left: 50rem;
        margin-top: 5.6rem;
        font-size: 1rem;
        color: var(--text-body);
    }

    button {
            background: transparent;
            color: #000;
            border: 0;
            position: absolute;
            margin-left: 60rem;
            margin-top: 5.5rem;
        
        }
`